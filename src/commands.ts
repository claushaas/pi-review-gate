import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";
import type { RuntimeState } from "./types.js";

export function registerCommands(_pi: ExtensionAPI, _state: RuntimeState): void {
  // Command registration will be implemented in later roadmap steps.
}
